name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        working-directory: app
        run: npm ci

      - name: Build Next.js app
        working-directory: app
        run: npm run build

      - name: Prepare deploy directory
        run: |
          mkdir -p deploy/app
          cp -r app/out/* deploy/app/
          cp index.html deploy/
          cp NANIMONO_SALES_AGENT.pdf deploy/ 2>/dev/null || true
          touch deploy/.nojekyll

      - name: Verify _next exists
        run: |
          echo "=== deploy/app/_next contents ==="
          ls -la deploy/app/_next/ || echo "MISSING _next!"
          echo "=== deploy/.nojekyll ==="
          ls -la deploy/.nojekyll

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: deploy
      - id: deployment
        uses: actions/deploy-pages@v4
